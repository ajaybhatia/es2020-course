/**
 * Arrays in Javascript
 */

// Mixed values can be used as an elements of
// an array in ES6+
const arr = [37, 9, 1, 89, 94, 12];
console.log(arr);

// Indexing
console.log(arr[3]);

// Replace
arr[2] = 10;
console.log(arr);
